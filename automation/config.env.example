# ============================================================================
# Automation Configuration - Plan A
# ============================================================================
# Copy this file to .env and update with your actual values
# These settings control the delta apply orchestrator and status change reporter

# ============================================================================
# DELTA APPLY ORCHESTRATOR
# ============================================================================

# Directory to watch for Excel files
# Can be OneDrive local sync folder or network/local folder
EXCEL_WATCH_DIR=./excel_files

# Path to delta apply script (relative to repo root)
DELTA_APPLY_SCRIPT=src/wpr_agent/cli/delta_apply_influx.py

# Excel sheet name to process
EXCEL_SHEET_NAME=WP_Overall_Order_Report

# Scheduled time for daily delta apply (24-hour format HH:MM)
DELTA_APPLY_SCHEDULED_TIME=07:00

# Apply mode: "online" (real updates) or "dry-run" (simulation only)
DELTA_APPLY_MODE=online

# ============================================================================
# STATUS CHANGE REPORTER
# ============================================================================

# Email recipients for status reports (comma-separated)
STATUS_REPORT_EMAILS=your.email@example.com,teammate@example.com

# SMTP Server Configuration (Office 365)
SMTP_SERVER=smtp.office365.com
SMTP_PORT=587
SMTP_USER=bmalaraju@infinite.com
SMTP_PASSWORD=BroWar@199720261

# Sender email (defaults to SMTP_USER if not set)
SENDER_EMAIL=bmalaraju@infinite.com

# Filter for Nokia projects (case-insensitive substring match)
NOKIA_PROJECT_FILTER=nokia

# ============================================================================
# GENERAL CONFIGURATION
# ============================================================================

# Log directory for orchestrator and reporter
LOG_DIR=automation/logs

# ============================================================================
# OPENPROJECT & INFLUX CONFIGURATION
# ============================================================================
# These should already be in your main .env file
# Include them here if running scripts standalone

# OPENPROJECT_URL=https://your-openproject-instance.com
# OPENPROJECT_API_KEY=your_api_key_here

# INFLUX_URL=https://your-influx-instance.com
# INFLUX_TOKEN=your_influx_token_here
# INFLUX_ORG=your-org
# INFLUX_BUCKET=your-bucket
